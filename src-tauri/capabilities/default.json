{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permite apenas que a shell feche a propria janela (botao Fechar do modo Steam).",
  "windows": ["main"],
  "permissions": [
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
