# AGENTS.md — Illusion Fight Site

## Mandatory workflow for every code change

No exceptions. Every modification to any game file:

> **⚠️ O VERSION NUMBER VEM PRIMEIRO — NÃO PULE ESTA ETAPA**

1. **🔴 BUMP THE VERSION** — Abra `C:\Users\isaia\Downloads\BRANDS\Lutas de Ilusão\SiteLDI\src\config\version.js` e **INCREMENTE** a versão do jogo/site modificado.
   > **Regra absoluta:** toda task que toca **qualquer arquivo** do projeto sobe `SITE_VERSION` patch obrigatoriamente, sem exceção. Se a task também mexe em um jogo específico, sobe a versão do jogo **e** a do site.
2. **🔴 Update `SITE_MAP.md`** — Atualize o número da versão na tabela de rotas
3. **`npm run build`** — if it fails, fix before proceeding. `sourcemap: true` é obrigatório em todas as builds até o lançamento (Novembro 2026). Nunca remover sem autorização explícita de Isaias.
4. **`git add -A && git commit -m "<desc> + vX.X.X"`**
5. **`git push`**
6. **`npm run deploy`**
7. **Verify** the deploy published without errors

> **📋 AO FINAL, O RELATÓRIO DEVE INCLUIR:**
> ```
> | Versão | Antes | Depois |
> |--------|-------|--------|
> | SITE_VERSION | X.X.X | → **Y.Y.Y** |
> ```

## Stack

- **Vite 8** + **React 19** (JSX only, no TypeScript)
- **React Router 7** (client-side routing)
- **Zustand 5** (state management per game)
- **Framer Motion 12** (animations)
- **Supabase** (`@supabase/supabase-js` v2) — auth, realtime, save persistence
- **gh-pages** — deploy to GitHub Pages branch
- **react-helmet-async** — per-page `<title>`
- **react-markdown** — book chapter rendering
- **Zero CSS-in-JS** — every component has a matching `.css` file. No `style={{}}` in JSX.
- **No lint, no typecheck.** Only verification is `npm run build`.

## Environment

- `.env` sets `VITE_DEBUG=true` (dev)
- `.env.production` sets `VITE_DEBUG=false`
- **Domínio oficial:** `https://illusionfight.com/` (GitHub Pages + CNAME)
- Vite base: `/` (custom domain)
- Supabase project: `dvxfrzixtetdzmdrzkpx.supabase.co` — client initialized in `src/lib/supabase.js` with anon key

## 🤖 Custom Agents

O projeto possui agentes personalizados em `.github/agents/`:

| Agente | Arquivo | Trigger | Descrição |
|--------|---------|---------|-----------|
| **LDI** | `.github/agents/ldi.agent.md` | `/ldi` | Workflow completo de versionamento + deploy. Sem micro-commits. Toda alteração → bump version → build → commit → push → deploy + relatório. |

## Deploy commands (must run in this order)

```
npm run build        # vite build → dist/
npm run deploy       # predeploy runs build, then gh-pages pushes dist/ to gh-pages branch
git push             # push main branch source
```

`npm run dev` starts the Vite dev server. `npm run preview` previews the production build locally.

## SPA on GitHub Pages

`public/404.html` catches 404s, extracts the original path, and redirects to `/?/<path>`. A script in `index.html` head restores the clean URL via `history.replaceState`. Do not remove or break either file.

## Version constants (every game has its own)

| Game | Constant | File |
|---|---|---|
| Site global | `SITE_VERSION` | `src/config/version.js` |
| Pesadelo Particular | `PP_VERSION` | `src/config/version.js` |
| Lendas do LDI | `LDI_VERSION` | `src/config/version.js` |
| Jack Dream Candy | `JACK_VERSION` | `src/config/version.js` |
| LDI Gangues | `GANGUES_VERSION` | `src/config/version.js` |
| Arena LDI Tatics | `TATICS_VERSION` | `src/config/version.js` |
| MiniGames | `MINIGAMES_VERSION` | `src/config/version.js` |
| Top Trumps SP | `TS_VERSION` | `src/config/version.js` |
| Top Trumps MP | `TM_VERSION` | `src/config/version.js` |

> **⚠️ Atenção:** A partir de Julho 2026, TODAS as versões estão centralizadas em `src/config/version.js`. As versões individuais nos arquivos de jogo agora são imports desse único arquivo.

Each logs to console on mount: `[LDI] versão carregada: X.X.X`, etc.



At the end of every task, deliver a **report table** with the version changes **highlighted**:

```
| Arquivo | O que mudou | Versão |
|---|---|---|
| `src/config/version.js` | TATICS_VERSION bump | X.X.X → **Y.Y.Y** |
| `src/config/version.js` | SITE_VERSION bump | X.X → **Y.Y** |
| `SITE_MAP.md` | Versão atualizada | ✅ |
| **Commit** | `abc1234` — `desc + vY.Y.Y` | ✅ |
| **Deploy** | Status | ✅/❌ |
```

## Conduct rules

- **Never** overwrite entire arrays — add items instead
- **Never** remove diagnostic `console.log`s unless asked
- **Never** use inline `style={{}}` for visual properties — keep it in CSS
- **Always** verify no existing files were destroyed before deploy
- **Always** check ALL cards are present when modifying `src/pages/Extras.jsx`
- **Always** read `SITE_MAP.md` before changes that affect routes or versions
- **Read `SITE_MAP.md` first** for route tables, component inventory, data file locations, and z-index layer map before navigating the codebase
- **Arquivos proibidos — nunca tocar**:
  - `e2e/routes.spec.js`
  > ArenaTestbed não tem mais restrição de arquivos proibidos (revisado em 11/07/2026 — V1 do combate foi deletado, V2 é a única versão ativa, protótipo em desenvolvimento normal).

## Decisões e Hurdles Documentados

Registro vivo de problemas já resolvidos no projeto para o agente não repetir os mesmos erros.

- **Canvas height não se comporta como div em flex** — sempre medir o pai, nunca o canvas
- **FK profiles_id_fkey** — insert em profiles só após SIGNED_IN, nunca no signUp direto
- **emailRedirectTo** deve ser URL fixa `https://illusionfight.com/login`, nunca `window.location.origin`
- **Nunca migrar para nova biblioteca de rendering sem ganho visual comprovado** (lição Pixi.js)
- **Sempre `maybeSingle()`** em vez de `single()` no Supabase para evitar erro em resultado vazio
- **Glob path quebra silenciosamente ao mover componente de pasta** — `import.meta.glob` usa caminho relativo ao arquivo atual. Se o componente é movido (ex: `src/pages/` → `src/pages/content/`), o glob `'../data/**/*.md'` não acha mais nada, mas não dá erro — só retorna vazio. Sempre verificar paths de `import.meta.glob` após refactors que movem arquivos.
- **notificationManager.queue persiste em localStorage entre sessões** — achievements enfileirados por um usuário logado permanecem na fila após logout. Quando o guest continua navegando, `UnifiedNotification` encontra as notificações stale e exibe popup de achievement. A correção é limpar (`clearByType('achievement')`) na transição `user → null` em `AchievementsContext`. As notificações LDI tip não são afetadas.
- **LINGUAGEM VISUAL ÚNICA — a base é a navbar** — o portal tem UMA linguagem, extraída da navbar/drawer e codificada em `src/styles/design-system.css` (importado por `index.css`). O visitante tem que sentir que está no mesmo aplicativo o tempo todo: nada de cada área ter paleta, fonte e efeito próprios. Antes eram 4 âmbares (`#f5a623`, `#e8853a`, `#f4a227`, `#ffae32`) e 4 cianos (`#00b4d8`, `#00eeff`, `#00e5ff`, `#18dafb`) espalhados — hoje são `--if-amber` e `--if-cyan`/`--if-teal`. **Nunca escreva hex novo numa página**: use os tokens. Os gestos de assinatura são o canto chanfrado (`clip-path` cortando o topo direito, nunca `border-radius`), o eyebrow mono `IF // ALGO`, o índice numerado, a varredura de luz no hover e a barra ativa `inset 2px`. Primitivas prontas: `.if-panel`, `.if-btn`, `.if-field`, `.if-eyebrow`, `.if-item`, `.if-badge`, `.if-divider`, `.if-stagger`.
- **Cor em canvas não aceita `var()`** — `ctx.fillStyle = 'var(--if-cyan)'` não pinta nada e falha em silêncio. A unificação de paleta vale para CSS e para `style={{}}` de JSX; motores de jogo que desenham em canvas (`drawCombatBoard.js`, `particles.js`, `GridCanvas.jsx`) continuam com hex literal de propósito.
- **i18n é carregado por idioma e por área** — `src/i18n/locales.js` expõe `carregarCore(locale)`, `carregarArea(area, locale)` e `areaDaRota(pathname)`; o `LanguageProvider` baixa só o núcleo do idioma ativo e puxa a área pesada quando a rota entra nela. Antes os 3 idiomas inteiros entravam no bundle (~356K) para servir um. Estrutura: `i18n/core/<lang>.json` (navegação, home, histórias, conta — sempre), `i18n/games/<lang>.json` (strings de dentro dos jogos, inclui pp e toptrumps), `i18n/prototype/<lang>.json`, e `gangues-<lang>.json` à parte via `useGanguesI18n()`. **Chave nova vai no arquivo da área, nunca num monolito**; e tem que existir nos 3 idiomas.
- **O PORTAL É MOBILE ONLY — uma visão só, a do celular** — não é mobile-first, é mobile e ponto. Não importa a plataforma que acessa: num monitor de 1920px o site é o MESMO app de celular, numa coluna centralizada, com o resto da tela servindo de moldura. Não existe visão desktop, nem "versão desktop do componente X", nem breakpoint que revela algo a mais em tela grande. A coluna é `#root` em `src/index.css` (`--app-w: 480px`, NUNCA mude) e toda página herda — não repita `max-width` por página. Vale para TODA página, jogo, overlay e componente.
- **Mobile only: as três armadilhas** — media query, unidade `vw` e `position: fixed` medem o VIEWPORT, não a coluna; num desktop enxergam 1920px e quebram a visão única. (1) `@media`: `max-width` ≥ 480 é sempre verdadeiro dentro da coluna → desembrulhe o bloco; `min-width` ≥ 480 é estilo desktop → apague. Só sobrevive query abaixo de 480 ou não-dimensional (`prefers-reduced-motion`, `orientation`). (2) `vw`: `78vw` num desktop = 1497px — use `calc(78 * var(--app-vw) / 100)`, inclusive dentro de `clamp()` de fonte. (3) `position: fixed`: cola na borda da tela — confine com `left/right: var(--app-gutter)` (bloco pronto no `index.css`); painel ancorado à direita, como o drawer, leva só `right`. Foi assim que a Rádio Nina parou de atravessar o monitor inteiro. Detalhes na Bíblia §4.
- **Kernel Games: padding mínimo, espaço máximo** — dentro do container portrait, cada pixel conta:
  - `.sr-arena` / arena: padding 8px (não 16px). O cálculo de tileSize no JS deve bater com o CSS (`clientWidth - 16` para padding 8px).
  - `.sr-hud` / `.cp-hud`: padding 6px 12px (não 8px 16px)
  - `.cp-game-body`: padding 8px 6px 6px (não 16px 14px 12px)
  - Margens de elementos de menu: 24px (não 40px)
  - Keyboard: sem max-width no container, teclas sem max-width individual, `flex: 1` distribui igualmente, min-height 44px
- **Kernel Games: CSS mobile-first NÃO é media query no fim** — o layout deve ser pensado para retrato desde o início. Adicionar `@media (min-width: 600px)` com overrides no final do CSS é trabalho superficial e NÃO será aceito. O container .kg-page com max-width já resolve a adaptação.
- **CSS de jogo nunca pode usar seletor de elemento global** — regras como `header {}` vazam porque os CSS importados pelo Vite são globais. Sempre limitar pelo wrapper do jogo (`.kp-wrapper header`) e manter classes prefixadas; um `header { display:flex }` do Kernel Panic quebrou o cabeçalho do lobby multiplayer.
- **Cascata CSS deve ser validada no bundle final do Vite** — CSS importado por um componente filho pode aparecer antes do CSS base importado pelo componente pai. Um modificador com a mesma especificidade pode funcionar numa concatenação manual e ser sobrescrito em produção. Modificadores críticos de layout devem usar especificidade suficiente ou ficar depois da regra base; o teste deve carregar o CSS gerado em `dist/assets/` e conferir os estilos computados e o comportamento real no Playwright. Caso conhecido: `.gang-page` sobrescreveu o scroll mobile de `.gang-page--training` na Zona de Treinamento.
- **Kernel Games: hierarquia de botão Voltar** — dentro do gameplay: `setPhase('select')` + cleanup (volta ao menu de dificuldade). No menu de dificuldade: `onBack` prop → `navigate('/games')` (volta ao catálogo Kernel Games).
- **Isaias exige revisão visual real, não checklist burocrática** — toda mudança de CSS/layout deve ser pensada e testada visualmente. Valores arbitrários sem intenção de uso de espaço serão rejeitados. Se o agente não tem certeza do resultado visual, deve ler os CSS existentes e entender o fluxo de layout antes de editar.
- **ID de conteúdo é NÚMERO, nunca nome/slug** — vale pra TODO catálogo de dado do jogo (personagens, itens, poções, equipamento, inimigos, cartas...). O código só referencia por `id` numérico; um `slug` opcional serve só de leitura humana no arquivo de dados. O nome exibido mora no i18n (`games.<jogo>.<catalogo>.itens.<id>` nos 3 idiomas), então renomear "Colete de Couro" → "Colete de Pano" é editar só o JSON de idioma, sem tocar em código nem em id nem em save. Faixas de id separadas por catálogo quando dois alimentam o mesmo lugar (ex. Gangues: consumível 1–99, equipamento 101+). Precedente: catálogo dos 30 personagens (`ldi_gangues_30_personagens_v1.json`, ids 1–30). Erro cometido e corrigido em set/2026 no 1º catálogo de equipamento (ids eram `colete_couro` etc.).
- **Deploy sem commit = deploy perdido** — o workflow obrigatório é bump → build → commit → push → deploy. Pular commit/push faz o código-fonte não acompanhar o build publicado. NUNCA pular `git add -A && git commit && git push` antes de `npm run deploy`.
- **Progressão de atributo do Gangues: SEMPRE +1 flat por nível, nunca custo escalonado** (revertido pelo Isaias em 12/09/2026 — ele mesmo tinha pedido o custo escalonado antes, se arrependeu depois de jogar de verdade). Histórico completo: jan/2027 (`84094fe5`) criou `custoAtributoGangues` (cada ponto de atributo custando mais XP quanto mais alto já estava, inspirado no sistema de pontos do 3D&T) mas a função nunca era usada de verdade pelo gerador do catálogo — só decorativa. Em set/2026 (`a913e21`) o Isaias pediu pra "fazer valer de verdade" em vez de apagar como código morto, aceitando explicitamente que "vai ter nível que nenhum atributo vai subir". Depois de jogar (`Trinca NV96-99 zerado`, sem sentir progressão nenhuma), reverteu 100%: "isso aqui é estilo Ragnarok, todo nível tem que subir atributo, como era antes... descarta totalmente essa ideia, apaga tudo, não deixa código morto". Revert final: `custoAtributoGangues` deletado de `ganguesCharacters.js` (não só comentado/desativado); `scripts/gangues-regen-catalog.cjs` reescrito pra flat +1/nível (sem banco de XP) e catálogo `ldi_gangues_30_personagens_v1.json` regenerado (0 gaps confirmados nos 30 personagens); `GANGUES_CHEFE_BUDGET` (`ganguesEncontros.js`) e preços de equipamento A/H/D (`ganguesEquip.js`) voltaram aos valores pré-84094fe (calibrados pra "1 ficha nível N = N pontos"); tag "sem_atributo" removida de `GanguesVictoryReport.jsx`/`GanguesProgressionFlow.css`/i18n×3 (não fazia mais sentido existir). PV/PM como atributos separados (também de 84094fe) **não** foi revertido — só o custo escalonado era o problema, não a separação PV/PM. Não reintroduzir custo por atributo sem pedido explícito e sem playtest real confirmando que funciona — já foi tentado duas vezes.
- **Vinheta de abertura cobre TODA carga, detectando cache de verdade — não "1x por dia"** (pedido do Isaias em 12/09/2026: reportou ver texto cru piscando antes do site carregar "de vez em quando"; correção de rumo do próprio Isaias depois: a 1ª versão do fix usava `localStorage['ldi-intro-day']`, um gatilho por calendário que não tem nada a ver com cache real — ele pediu pra trocar por detecção de cache de verdade). Causa raiz: `#root` chega pro navegador com HTML estático de SEO (gerado por `scripts/prerender-routes.js`, texto sem estilo real, existe só pra Google/crawlers) até o bundle React montar por cima; a vinheta (`#ldi-intro` em `index.html`) existe justamente pra cobrir esse vão. Mecanismo final: o chunk de entrada tem hash de conteúdo no nome (`index-HASH.js`, igual em toda rota — Vite não muda o hash se o código não mudou), `scripts/prerender-routes.js` extrai esse hash e grava em `<meta name="ldi-build">`; o script da vinheta compara com `localStorage['ldi-intro-build']` (hash do último build que **terminou de montar com sucesso**, gravado só no evento `ldi:ready` real, nunca no teto de segurança). Hash bate = bundle quase certo já em cache (deploy não mudou) → modo silencioso, sem som, `MIN=0`, some assim que `ldi:ready` dispara. Hash não bate ou nunca visitou = cache miss de verdade (deploy novo ou 1ª visita) → show completo (logo animado, som, mínimo 2s/600ms reduced-motion). Teto de 4s como rede de segurança nos dois casos. Não trocar essa detecção por calendário/sessão de novo — o ponto inteiro é reagir ao cache real do navegador, não a um relógio.
- **Todo overlay `position:fixed` de tela cheia precisa de `overscroll-behavior:none` (+ `touch-action:none` se bloqueia interação)** — regra geral, não só do combate do Gangues. `overflow:hidden` no elemento/ancestral só bloqueia scroll PROGRAMÁTICO (JS/wheel); num toque real (Android/iOS) o dedo ainda consegue arrastar a página e disparar o bounce/rubber-band nativo do navegador, que aparece como "scroll" por um instante (lateral ou embaixo) mesmo sem overflow de layout nenhum. Já apareceu 2x: no log de combate do Gangues (`.gang-log-area`, `overflow-y:auto` sem a propriedade) e na vinheta de abertura + trava de orientação (`#ldi-intro`/`#ldi-rotate-lock` em `index.html`, `html.ldi-intro-on { overflow:hidden }` sem a propriedade — Isaias reportou "scroll lateral e embaixo" ao recarregar). Ao criar qualquer overlay fixed de tela cheia (modal, splash, lock screen), incluir `overscroll-behavior:none` de cara — não esperar reportarem.
- **Scroll indevido não é sempre overflow de CSS — pode ser scroll-chaining sem `overscroll-behavior`** (Isaias reportou scroll durante a batalha do Gangues no Galaxy A52, 12/09/2026). Antes de mexer em layout, testei via debug hook temporário (`window.__ganguesDebugFightN`, removido depois) o combate 1v1 e 6v6 "Briga em Multidão" — em ambos o `.gang-combat` (flex column, `max-height:800px`, `overflow:hidden`) coube o conteúdo certinho, sem overflow real. A causa era outra: `.gang-log-area` (chat do combate) tem `overflow-y:auto` sem `overscroll-behavior` — um swipe que chega na borda do log "vaza" pro documento e o Android dispara o bounce/pull-to-refresh nativo na tela inteira, mesmo com `.gang-page`/`.gang-combat` em `overflow:hidden` (que só bloqueia scroll programático/wheel, não o scroll-chaining por toque). Fix: `overscroll-behavior: contain` + `-webkit-overflow-scrolling: touch` em `.gang-log-area`, e `overscroll-behavior: none` no `.gang-page` como defesa extra — mesmo padrão já usado em `GanguesAlbum.css`/`Tamagoshi.css`. Lição: qualquer área com `overflow-y: auto` dentro de uma página mobile-only `position:fixed` PRECISA de `overscroll-behavior`, senão o scroll dela pode escapar pra página inteira em toque real (não reproduz em mouse/desktop).
- **`npm run deploy` LIMPA `dist/` INTEIRO — qualquer arquivo colado manualmente ali (fora de `src`/`public`) some no próximo deploy, sem erro nenhum** (bug real: a página privada `/preview-privado/webtoon-00-comparar`, `src/pages/preview/WebtoonCompare.jsx`, quebrou entre 12 e 13/09/2026 — Isaias usou com o sócio, no dia seguinte via ícone de imagem quebrada). Causa: `predeploy` roda `npm run build`, o Vite limpa `dist/` (`emptyOutDir` padrão) antes de gerar de novo; as 2 imagens dessa página (~90MB/~28MB, grandes demais pro git principal) nunca fizeram parte do build — tinham sido coladas manualmente em `dist/preview-privado/` uma vez e publicadas. Qualquer deploy de rotina depois disso (pra QUALQUER outra mudança do site, sem relação nenhuma com essa página) silenciosamente as apaga do ar. Recuperadas do próprio histórico do branch `gh-pages` (nunca é reescrito/squashado, cada deploy só soma um commit novo) — por isso um commit antigo com os arquivos originais sempre sobrevive lá, mesmo depois de sumirem do HEAD. Corrigido de vez: `scripts/restaurar-preview-privado.cjs` reextrai os arquivos de um commit fixo do `gh-pages` e recopia pra `dist/preview-privado/`, chamado no fim do `predeploy` em `package.json` — todo `npm run deploy` futuro se auto-recupera. Lição geral: **nunca** colar arquivo manualmente dentro de `dist/` esperando que sobreviva a um deploy normal; se for inevitável (arquivo grande demais pro git), automatizar a reposição no próprio `predeploy`, não confiar em alguém lembrar. Atualização 13/09/2026: Isaias pediu pra recortar as 2 imagens unificadas (um scroll gigante com todas as páginas coladas) em páginas individuais pra comparar com o sócio — dividido por altura igual usando a contagem de página já conhecida (32/37), confirmado visualmente que nenhum corte cai no meio de painel (a versão antiga inclusive bateu EXATO: 47360px ÷ 37 = 1280px, confirmando que essa é a altura real de página usada na montagem original). O script de restauração e o commit fixo do `gh-pages` foram atualizados pra guardar as 69 páginas individuais em vez dos 2 arquivos unificados.
- **Sistema de notificação: CTA de conta e "dicas" LDI Tip DESLIGADOS — achievement/troféu e Rádio Nina continuam intactos** (pedido do Isaias em 13/09/2026: achou o sistema, que ele mesmo pediu no passado, chato e invasivo demais jogando de verdade — "vira uma piscina de notificação"; temporário, ele quer repensar o conceito depois. Correção de escopo no mesmo dia: a 1ª versão desligou TUDO incluindo o toast de conquista — ele voltou pra deixar claro "não é pra mexer em conquistas/troféis, não é pra mexer na Rádio Nina, é os outros"). Arquitetura: 3 produtores (`AchievementsContext.jsx` push de `achievement`/`cta_conta`, `LDINotification.jsx` push de `ldi_tip` a cada 5min, `notificationStore.js`/Tamagoshi push de `ldi_tip`) alimentam uma fila única em `localStorage` (`notificationManager.js`), lida por `UnifiedNotification.jsx` (montado globalmente em `App.jsx`). A Rádio Nina é **completamente separada**: seu convite (`RadioNina.jsx`) escreve direto em `window.__ninaPendingNotification`, nunca passa pelo `notificationManager` — só é renderizado pelo mesmo `UnifiedNotification.jsx` (branch `nina_music`, prioridade sobre a fila). Gate final em `notificationManager.push()`: `TIPOS_DESATIVADOS = Set(['cta_conta', 'ldi_tip'])`, seletivo por tipo — `achievement` passa direto (grava no Supabase E mostra o toast, normal), só `cta_conta`/`ldi_tip` são descartados antes de entrar na fila. Reativar um tipo: tirá-lo do Set.
- **Gangues: switch da Briga em Multidão agora liga/desliga a qualquer momento (não trava mais depois da 1ª ação)** (pedido do Isaias, 13/09/2026: "liguei pra testar e não consegui desligar mais, não pode ser assim, o player tem que poder desligar"). Antes, `switchTravado` desabilitava o botão permanentemente assim que QUALQUER ação acontecia (ataque normal, usar item, ou avançar 1 rodada da Multidão) — de propósito, pra não ter que sincronizar os dois motores de combate (o normal, `useGanguesTurnMachine.js`, e o da Multidão, `ganguesBrigaMultidao.js`) no meio da luta, já que cada um mantém seu próprio `combatants`/HP em memória separada. A troca: `useGanguesTurnMachine` ganhou `syncFrom(estado)` (sobrescreve combatants/initiative/round/turnIndex inteiros — precisou virar `initiative` de `useMemo` fixo pra `useState` pra dar pra sobrescrever) e `ganguesBrigaMultidao.js` ganhou `iniciarBrigaMultidaoDeCombatentes(combatants, round)` (monta o estado da Multidão a partir dos combatentes JÁ preparados do motor normal, em vez de sempre resetar do time cru salvo). `alternarMultidao()` (`useGanguesModoMultidao.js`) decide a direção: ligando, parte do estado vivo do motor normal se ele já andou; desligando, devolve o estado vivo da Multidão pro motor normal. Só bloqueia o botão durante a revelação de 1 rodada (`revelandoRodada`, evita alternar no meio de uma animação) ou depois da luta acabar (`result`) — nunca mais um "trava pra sempre". Os dois motores usam o mesmo `prepare()` (mesma forma de `combatant`: key/pv/pm/statuses/specialState), por isso o sync é uma cópia direta, sem conversão.
- **Gangues: tutoriais "já visto" precisam ser escopados por save, nunca localStorage global** (bug real: Isaias excluiu a gangue, criou outra, e nenhum tutorial reapareceu — descoberto junto com o bug do switch da Multidão acima). As 4 flags autocontidas (`ldi-gangues-cena-intro-vista` em `GanguesCena.jsx`, `ldi-gangues-combate-tutorial-v4-visto` em `GanguesCombatTutorial.jsx`, `ldi-gangues-multidao-tutorial-v2-visto` em `GanguesMultidaoTutorial.jsx`, `ldi-gangues-multidao-blink-visto` em `ganguesCombatPresentation.js`) usavam uma chave de `localStorage` FIXA — persistiam pro navegador inteiro, pra sempre, e nenhum fluxo de excluir/criar/trocar save (`ganguesSaveSlice.js`, `ganguesStorySlice.js`) tocava nelas. Corrigido: toda chave virou `` `${CHAVE_BASE}:${saveId || 'guest'}` `` (saveId lido de `store._saveId`) — gangue nova ganha `saveId` novo, então os tutoriais voltam a aparecer sozinhos, sem precisar limpar nada no delete/create. Fail-open continua igual (erro de leitura = mostra o tutorial, nunca esconde). Padrão pra QUALQUER flag "já visto"/"tutorial completo" nova no Gangues: sempre escopar por `saveId`, nunca usar uma chave crua.
- **Framer Motion: `key` novo sozinho NÃO impede animação de montagem — precisa de `initial={false}`** (Isaias reportou 2 bugs na navegação de interiores do Gangues em 13/09/2026: preso numa porta que só passa numa posição certinha, e ao sair "sou lançado num lugar vazio antes de assentar"). Causa 1 (porta estreita): as portas de saída dos interiores (`interiores.js`) tinham vão de parede de só ~70px — com `PLAYER_RADIUS=18` e movimento em grade `TILE=20` (`ganguesCenaMotor.js`), sobravam só 2 colunas exatas pra atravessar sem colidir; alargado pra 160px (~7 colunas) nas 7 portas/saídas afetadas. Causa 2 (teleporte): `GangMarker` (`GanguesCenaAtores.jsx`, `motion.div` com `animate={{left,top}}`) troca de `local` (rua↔interior) muda `player` de um espaço de coordenada pequeno (mundo do cômodo) pra um gigante (`WORLD` da rua) na mesma atualização de estado — sem cuidado, o Framer Motion anima a transição INTEIRA entre os dois espaços incompatíveis, o glitch visual relatado. Primeira tentativa (dar `key={local?...}` ao `GangMarker` pra forçar remontagem) **não bastou**: confirmado via Playwright com polling `requestAnimationFrame` no `style.left/top` que mesmo remontado o componente ainda animava a partir de um valor medido do DOM (perto de 0,0), não do alvo. Fix real: `initial={false}` no `motion.div` — só afeta a PRIMEIRA pintura de cada montagem (o passo a passo normal, na mesma instância, continua suave via `animate`+`transition`). Qualquer `motion.div` de posição que pode remontar com coordenadas de espaços diferentes (troca de mapa/cena/mundo) precisa de `initial={false}`, não só de uma `key` nova.
- **Gangues: Pista era fácil demais — a Briga em Multidão quase nunca ativava porque o teto de corpos ficava travado pelo tamanho do time, não pelo `max` do território** (pedido do Isaias, 13/09/2026, jogando de verdade com playstyle "farmador clássico": "não é pra funcionar mal, tá muito fácil mesmo... é jogo de gangue, não RPG clássico, precisa ter mais briga em multidão" e "quem tenta avançar sem grindar tem que ficar perdendo tempo, não progredir"). Causa raiz: o time de jogador na Pista fica travado em 2 fichas (3 com reputação ≥50) o jogo praticamente inteiro (`getGanguesRosterLimitComHistoria`), e o teto real de corpos em `gerarBandoInimigo` (`ganguesEncontros.js`) é `min(config.max, playerTeam.length + 2)` — com time de 2, isso já batia em 4 ANTES mesmo de tocar no `max` do território (2+2=4), então só subir `GANGUES_TERRITORIO_ENCONTRO.pista.max` não mudava nada na prática. A maioria do conteúdo de treta da Pista (beco, rinha, sinal, ferro, túnel, pós-muro, galpão) usa `revezamento`, que trava em 1-2 corpos por design ("estilo Pokémon", farm rápido) e não passa por essa fórmula — só 5 POIs soltos (beco_2, beco_3, sinaleiro, rasteira_velha, galpao_m2) usam `gerarBandoInimigo` de verdade; ficaram DE FORA desse ajuste de propósito (não mexi no `budgetPorCorpo` do revezamento — se quiser esses mais difíceis também, é outro pedido). 3 ajustes: (1) `useGanguesModoMultidao.js`: piso de ativação 6→5 combatentes totais. (2) novo campo opcional `folgaMax` em `GANGUES_TERRITORIO_ENCONTRO` (usado como `config.folgaMax ?? 2` na fórmula do teto) — sobrescreve o `+2` só pra Pista (`folgaMax: 3`), liberando o teto de corpos de verdade (2+3=5) sem tocar no cálculo de nenhum outro território; `max` subiu de 4→5 em conjunto (senão o `max` antigo virava o novo teto). (3) `GANGUES_TERRITORIO_STEP.pista` 0→0.02 (ainda abaixo do degrau da Feira, 0.03 — mantém a escada de dificuldade subindo território a território). Simulado: chance de ativar Multidão nessas 5 tretas soltas foi de ~33% pra ~75% com time de 2, 100% com time de 3. Números são chute educado, não simulação formal — Isaias vai jogar e pode pedir mais ajuste (inclusive nas tretas de revezamento, que ficaram de fora).
- **`position:fixed` dentro de um card animado por Framer Motion fica PRESO no stacking context dele — `z-index` alto não resolve** (bug real achado 13/09/2026 escrevendo os tutoriais contextuais de descanso/Clube da Luta do Gangues: `GangTip`, `position:fixed; z-index:9998`, renderizava visualmente por cima do modal, mas clicar no botão "próximo" fechava o modal INTEIRO em vez de avançar o tutorial). Causa: qualquer ancestral com `transform` (todo `motion.div` do Framer Motion aplica um em animação — `.gang-cena-modal-card` no caso) vira um **containing block novo pra elementos `position:fixed`** — o descendente `fixed` deixa de ser fixo à viewport e passa a competir de `z-index` só DENTRO do stacking context desse ancestral, não mais globalmente. Um `z-index:9998` que deveria vencer qualquer coisa da página fica preso perdendo pra um backdrop irmão (`.gang-cena-modal-bg`) que está FORA desse stacking context capturado. Sintoma sutil: o elemento aparece corretamente na tela (parece que "z-index alto funcionou"), mas o CLIQUE vai pro elemento errado — muito fácil de não notar sem testar a interação de verdade. Corrigido na raiz: `GangTip.jsx` agora usa `createPortal(..., document.body)`, escapando qualquer ancestral transformado — protege todo uso futuro do componente, não só o caso que quebrou. Regra geral: **todo overlay `position:fixed`/`position:absolute` com z-index alto que pode ser montado dentro de algo animado por Framer Motion (ou qualquer `transform`/`filter`/`will-change:transform`) deveria usar portal pro `<body>`**, não confiar só no z-index.
- **Trava de orientação — o produto inteiro é só vertical, nunca horizontal** (pedido do Isaias em 12/09/2026, depois de girar o celular e a UI quebrar). Não existe API confiável de orientation-lock pra site comum: `screen.orientation.lock()` só funciona em PWA instalada em fullscreen e só em Chrome/Android, falha silenciosamente em todo o resto. Solução: overlay bloqueante em `index.html` (`#ldi-rotate-lock`), fora do React, ativado via `@media (orientation: landscape) and (hover: none) and (pointer: coarse)` — o filtro `hover:none + pointer:coarse` restringe a touchscreen (celular/tablet físico); um monitor de desktop widescreen também é `orientation: landscape` sempre, então **nunca** usar só `orientation: landscape` sozinho ou isso quebra a visão desktop (que já é a coluna mobile centralizada, ver regra mobile-only). Texto pt/en/es escolhido por `localStorage['ldi-locale']`, igual ao script da vinheta `#ldi-intro` — esse trecho vive fora do i18n React de propósito, roda antes do bundle carregar. `manifest.webmanifest` também ganhou `"orientation": "portrait-primary"` pra quando o app é instalado como PWA (efeito real só em Android/Chrome). Não remover nem trocar por JS orientation-lock sem entender esse histórico.
- **Rádio Nina: prefetch da próxima faixa NÃO respeita mais "economia de dados" — travava a rádio em segundo plano** (pedido do Isaias, 13/09/2026: "toda hora tenho que ligar a tela e voltar pro app pra próxima música tocar... quando eu volto ele começa a próxima música"). Sintoma bateu certinho com a hipótese: a troca de faixa (`avancar()`/`tocarIndice()`, `useRadioNina.js`) rodava em segundo plano (senão a "próxima" música não estaria pronta assim que ele reabria), mas o `play()` da faixa nova ficava sem tocar até o app voltar pro primeiro plano. Causa provável: `prefetch()` baixa a PRÓXIMA faixa com antecedência (assim que a atual começa a tocar) só pra fazer o Service Worker (`sw.js`) gravar ela no Cache Storage (disco) — isso faz a troca de faixa depender só de leitura de disco, sem rede, o que deveria funcionar igual em 1º ou 2º plano. Só que o `prefetch()` tinha um `if (navigator.connection?.saveData) return` — com o "economia de dados" do Android ligado (comum no público do site), o prefetch NUNCA rodava, então a faixa nova só ia pra rede na hora exata da troca; rede de aba em segundo plano/tela apagada é despriorizada/atrasada pelo navegador, então esse fetch novo trava sem soltar nem "canplay" nem erro até o app voltar a primeiro plano (o que dispara o `retomar()` do listener de `visibilitychange`/`focus`, aí sim a música toca). Corrigido: removida a checagem de `saveData` do `prefetch()` — o total de dados baixados no fim das contas é o mesmo, a única diferença é baixar um pouco antes (com a aba ainda em 1º plano) em vez de exatamente na hora da troca. Adicionado também um vigia periódico (`setInterval` de 8s, throttled pelo navegador pra ~1x/min em 2º plano, mas ainda assim uma rede de segurança) tentando `audio.play()` de novo sozinho enquanto `querTocarRef` seguir verdadeiro — cobre qualquer outro caso em que nenhum evento (`canplay`/`loadeddata`/`visibilitychange`/`focus`) dispare a retomada. Limite real: navegador pode congelar a aba inteira depois de tempo demais em 2º plano — isso não é 100% contornável via JS de site comum (diferente de app nativo), mas as duas correções juntas devem cobrir a esmagadora maioria dos casos reportados.
- **Gangues: 3 bugs de UI/estado achados numa leva só, 13/09/2026 — todos do padrão "o código assume um caso que não é sempre verdade"**. (1) `.gang-auto-sair` (botão de sair do modo automático) usava `top: 60px` fixo — o Isaias reportou ele tampando a barra de PV do roster do jogador, "a ÚNICA coisa que dá pra monitorar em modo automático". Causa: o roster do jogador fica logo abaixo da barra do topo (`GanguesCombatTopBar` → `GanguesCombatRoster side="player"`, `GanguesCombat.jsx`) e sua altura varia com o tamanho do time/quebra de linha — um número fixo nunca ia "sempre garantir" (pedido explícito dele) que o botão ficasse abaixo. Corrigido com medição de verdade: `GanguesCombatRoster` virou `forwardRef`, `GanguesCombat.jsx` mede a borda de baixo do roster do jogador com `ResizeObserver` (`useLayoutEffect`, sem flash de 1 frame) e aplica o `top` calculado inline; o `top: 128px` do CSS virou só fallback do 1º frame. (2) Dica `hint_sucata_falta` ("falta um pedaço de sucata, tá no fundo do ferro-velho") em `GanguesCena.jsx` checava só `store.inventario[13] < 2`, sem checar SE ainda sobrava fonte pra buscar — se o jogador falhou a gazua do POI `ferro` (perde aquele pedaço de sucata pra sempre, vira treta em vez de item) e já tinha pego o `achado` (a outra fonte, sem falha possível), a dica continuava apontando pro "fundo do ferro-velho" mesmo com esse lugar já esvaziado — mentira pro jogador, reportado como "eu nunca vou ter ela, deveria simplesmente não aparecer". Corrigido: só mostra a dica enquanto `!prog.resolvidos.achado` (ainda existe fonte de verdade); virou `null` quando não sobra nada, em vez de apontar pra lugar vazio. ⚠️ ATUALIZAÇÃO NO MESMO DIA — softlock real corrigido: o Isaias confirmou que queria o ponto repetível em vez de travado pra sempre ("deixa repetível... só perde na hora, não pra sempre"). Em vez de mexer em `estadoPoi`/`ganguesCenaMotor.js` (mudança genérica, afetaria toda POI), a solução ficou cirúrgica: novo campo `semTravar: true` dentro de `falha.viraTreta` do POI `ferro` (`pois.js`) — threading via `iniciarTreta` → `setStoryTarget({cenaSemTravar})` → `useGanguesVictoryResolution.js`, que agora escolhe entre `store.revelarPoi` (não trava, só revela o mapa — usado quando `cenaSemTravar`) e `store.marcarPoiResolvido` (trava de vez, comportamento normal de qualquer outra treta). Como `resolvidos.ferro` nunca vira `true` enquanto o jogador só perde a gazua, `estadoPoi` devolve `'disponivel'` pra sempre — o ponto nunca "cai" do mapa, dá pra tentar de novo quantas vezes quiser, e SÓ quando a gazua é vencida de verdade (`recompensa.item:13` sai pelo caminho normal de sucesso, não pelo de falha) o POI é marcado resolvido igual antes. Zero mudança em POIs que não usam `semTravar` — o padrão fica disponível pra qualquer outro puzzle obrigatório futuro que não deva ter falha permanente. (3) Papo repetível (POI tipo `papo` com `repetivel:true`, ex. o informante Duda na birosca) nunca virava azul (`is-farm`/`farmCompleto`, a cor de "já visitado" — convenção do Isaias: "verde é o que precisa visitar, azul é o que já está visitado") depois de conversar. Causa: `resolver()` em `GanguesCena.jsx` só chamava `store.marcarPoiResolvido` pra POI NÃO-repetível; POI repetível de papo (sem vitória de combate no meio, diferente de treta repetível tipo `beco`/`rinha` que marca resolvido via `useGanguesVictoryResolution.js` depois de vencer) nunca tinha NENHUM caminho que setasse `resolvidos[poiId]=true` — ficava condenado a `revelarPoi` (só revela mapa) só quando a escolha trazia um `revela` explícito, o que nem sempre existe (caso do Duda). Corrigido: `resolver()` agora sempre chama `marcarPoiResolvido` — como `estadoPoi` (`ganguesCenaMotor.js`) já trata POI repetível como sempre `'disponivel'` independente de `resolvidos`, isso só liga o farejador visual (pino azul) sem travar a reinteração.
- **Gangues: farol universal vermelho/amarelo/verde nos pontos da rua, substituindo cor do território + azul de "farm"** (pedido do Isaias, 13/09/2026: "o que você não enfrentou tá vermelho, o que é opcional fica amarelo, o que já enfrentou fica verde... acho mais representativo"). Antes: pino disponível usava a cor do TERRITÓRIO (`--terr-cor`, ex. verde da Pista) e treta repetível já vencida virava azul (`is-farm`) — dois conceitos (identidade do bairro + status de progresso) misturados na mesma cor, que só fazia sentido com tutorial dedicado explicando ("verde é pra visitar, azul é já feito"). Validei a ideia com o Isaias antes de mexer: sinalizei que a troca custa a identidade visual do território no PINO (ela continua existindo no cenário/fundo, só sai do pino) — ele topou a troca. Pergunta em aberto que também resolvi com ele: treta repetível já vencida 1x (nunca vira `estado:'resolvido'` de propósito — ver `estadoPoi`/`estadoInternoPoi` em `ganguesCenaMotor.js`, fica sempre `'disponivel'` pra continuar farmável) — ele confirmou que conta como **verde** igual qualquer outro "já feito", sem cor own pra "posso repetir" (o badge giratório ↻ já existente continua sinalizando isso, separado da cor). Implementado: `farolDe(p)` nova em `GanguesCenaAtores.jsx` — retorna `'is-obrigatorio'` (vermelho, `--if-danger`), `'is-opcional'` (amarelo, `--if-amber`) ou `'is-feito'` (verde, `#6be07a` — mesmo tom já usado no checklist `is-feito`) a partir de `p.opcional` (já vem de cada definição de POI em `pois.js`, não precisou de campo novo) + `p.estado`/`p.farmCompleto`; aplicada tanto no `PinoAlvo` quanto no `EntryZone` (a zona de aproximação pisca na mesma cor). Fora do farol de propósito: navegação (porta/saída/volta/passagem, mantém o visual neutro/território de sempre) e o chefe (mantém a identidade vermelho-escuro/★ dedicada de showdown final, não vira "só mais um vermelho"). Tutorial de "cor dos alvos" (`GanguesAlvoTutorial.jsx`) reescrito com chave nova (`-v2-`, força reexibir pra quem já viu a versão antiga, já que o SIGNIFICADO da cor mudou) explicando o farol nos 3 idiomas. **Verificado ao vivo via Playwright** (criou gangue guest, entrou na Pista): confirmado vermelho na treta obrigatória "A boca do sinal" antes de resolver, amarelo no informante Duda e no Descanso (ambos opcionais, repetíveis) antes de interagir, e o pino do Duda virando verde de verdade depois de falar com ele uma vez — a cor nova bate exatamente com o comportamento esperado, inclusive empilhada corretamente com o fix de `marcarPoiResolvido` pra papo repetível (ver entrada acima).
- **Gangues: `useState(() => jaViu(saveId))` (lazy initializer) é frágil pra flag "já visto" escopada por save — não reavalia se `saveId` chegar depois** (Isaias, 13/09/2026: viu um tutorial reaparecer 1 vez isolada logo depois de um F5, sem repetir depois — suspeitou que fosse ligado ao reparo do save da sucata do mesmo dia). Investigação: o reparo da sucata só toca `cenaProgresso.pista.resolvidos.ferro`, nada a ver com nenhuma flag de tutorial — **não consegui confirmar uma ligação causal direta** com o caso relatado. Mas o padrão em si é genuinely frágil: os 6 tutoriais "só 1x por save" desta sessão (`GanguesCombatTutorial`, `GanguesMultidaoTutorial`, `GanguesAlvoTutorial` ×2, `GanguesDescansoTutorial`/`GanguesClubeTutorial` via `useTutorialPassos`, `GanguesVictoryReport` xpTip, `GanguesKoTutorial`) todos computavam "já visto?" com `useState(() => jaViu(saveId))` — um inicializador PREGUIÇOSO que só roda UMA VEZ, na montagem; se `_saveId` (Zustand) ainda não tivesse hidratado nesse exato instante, o check rodava sob a chave `guest` (sempre "não visto") e TRAVAVA nesse valor errado pra sempre — `useState` preguiçoso não reavalia sozinho quando a dependência muda depois. Na navegação normal (Lobby → Save Select → Território → Cena) isso não deveria acontecer, porque `selecionarSave()` seta `_saveId` de forma SÍNCRONA antes de qualquer tela do jogo montar — mas o padrão continuava uma armadilha esperando um caminho menos comum (Fast Refresh, alguma remontagem futura, etc.) pra disparar. Corrigido nos 6 pra sempre: `useState(true)` (assume "já visto" — evita pipocar o tutorial ERRADO enquanto ainda não confirmou) + `useEffect(() => setVisto(jaViu(saveId)), [saveId])` (reavalia toda vez que `saveId` muda de verdade). Única exceção deixada de propósito: `cenaIntroJaVista` em `GanguesCena.jsx` continua com o padrão antigo — ali o tutorial é um `GangDialog` de tela cheia (não um `GangTip` pequeno), e trocar pra um efeito criaria um flash visível (monta sem intro, intro pipoca 1 frame depois) pior que o risco teórico que resolve; `cena`/`storyTarget` também já estão garantidamente estáveis na montagem por vir de `setStoryTarget` síncrono. Lição geral: **toda flag "só 1x" que depende de um valor assíncrono (saveId, userId, dado de rede) tem que reavaliar via efeito, nunca só no inicializador preguiçoso do `useState`** — a única exceção aceitável é quando o valor de que depende é comprovadamente síncrono/estável na montagem E o custo de um flash visual do efeito é maior que o risco.
- **Gangues: save antigo travado na Oficina precisa de reparo NO CARREGAMENTO, não só o fix da causa raiz** (Isaias, 13/09/2026, mesmo dia do fix do POI "ferro": "eu já tinha pedido pra consertar isso... no meu save não consertou"). Lição geral: quando o bug já GRAVOU um estado ruim no save (não só um comportamento errado em tempo real), corrigir a causa impede casos NOVOS mas não desfaz o que já tá persistido — precisa de reparo explícito no load. Implementado em `loadStoryProgress` (`ganguesStorySlice.js`): se `cenaProgresso.pista.resolvidos.ferro` é `true`, a Oficina não fechou, e o jogador tem menos de 2× sucata (item 13), desfaz o `ferro` de volta pra não-resolvido antes de aplicar o save carregado no estado. Idempotente (roda toda vez que carrega, só tem efeito enquanto o travamento persistir) e zero efeito em quem nunca travou. Padrão a repetir: todo fix de "X ficou marcado errado no progresso" precisa se perguntar também "e quem já carrega esse X errado gravado?".
- **Gangues: glitch de câmera ao trocar rua↔interior não era regressão — era um bug IRMÃO nunca corrigido** (Isaias, 13/09/2026: achou que era o mesmo bug do GangMarker "voltando"). Investigação: o fix de 12/09/2026 (`3b0f14233`, ver entrada acima "Framer Motion: key novo sozinho...") só tratou o `motion.div` do BONECO (`GangMarker`). Só que `.gang-cena-world` (`GanguesCena.css:559`) — o container que a câmera desloca via `transform:translate3d` — tem `transition:transform .11s ease-out` (CSS puro, pensado pra suavizar a câmera seguindo o passo a passo normal DENTRO do mesmo espaço). Trocar `local` (rua↔interior) muda `camX/camY` de uma vez só num elemento que CONTINUA vivo (sem key, sem remount) — o navegador anima essa mudança de transform normalmente, varrendo a tela inteira entre dois espaços de coordenada incompatíveis (mundo pequeno do cômodo vs WORLD gigante da rua), o mesmo tipo de glitch do boneco só que na câmera. Nunca tinha sido notado/corrigido porque o boneco (mais chamativo) escondia o efeito visual da câmera fazendo a mesma coisa por baixo. Fix: mesma técnica — `key={local ? \`${local.id}-${local.comodo}\` : 'rua'}` no próprio `.gang-cena-world`, forçando remontagem (elemento novo nasce direto no transform final, sem transição CSS pra "de onde veio"). Regra geral pra lembrar: **qualquer transição CSS baseada em posição/transform que segue um valor computado a partir de coordenadas de espaços diferentes precisa da mesma proteção de key que já é convenção pra Framer Motion** — não é só motion.div que sofre desse bug, CSS transition simples tem exatamente o mesmo problema estrutural.
- **Gangues: encontro aleatório de rua rolava por TICK de movimento (110ms), não por "passo" no sentido intuitivo** (Isaias, 13/09/2026: "tá aparecendo muito, muito mesmo"). O comentário do código dizia "RARO de propósito... roll baixinho por passo" com `Math.random() >= 0.012` (1.2%) — matemática real: com STEP_MS=110ms e o roll rodando a cada tick que o jogador anda, a média pra disparar é `(1/0.012) × 110ms ≈ 9.2s` de caminhada contínua (bem abaixo do cooldown de 140 passos/~15.4s, ou seja, dispara quase toda vez que fica elegível de novo). Baixado pra `0.0022` (~0.22%), média `≈45s` — ainda pode pegar de surpresa numa caminhada longa (não virou zero), mas não interrompe o passeio a cada ~10 segundos. Lição: ao calibrar chance-por-tick num loop de `setTimeout` de alta frequência, calcular a média de tempo real (`(1/p) × STEP_MS`), não só "parece baixo" olhando o número da probabilidade isolado.
- **Gangues: 4 termos do sistema de personagem renomeados pra gíria de rua/crime, tema gangue** (pedido do Isaias, 13/09/2026, junto com a captura de tela da ficha do MURO — "quero mudar habilidade para corre... ataque para veneno... defesa para couro... deixa tudo temático desse tema de gangue crime rua"). Análise antes de aplicar: `Corre` (gíria de "hustle"/esquema de grana) NÃO batia com o que a Habilidade faz de verdade (decide quem age primeiro na iniciativa + bônus de precisão no ataque — reflexo/malícia na porrada, não hustle financeiro) — o Isaias concordou e escolheu **Malícia** no lugar. `Veneno` (Ataque) e `Couro` (Defesa) bateram certinho de cara — gíria real pra "gente perigosa"/"aguenta porrada". Pra "Poderes" (menu de habilidades especiais equipáveis) sugeri `Grau` (gíria de rua atual pra manobra/estilo) mas o Isaias preferiu algo mais neutro: **Talento**. Renomeado em `attr_labels` (H/A/D), `skill_desc` (`habilidade_full_convert`, `reduce_target_defense`, `ignore_def_pct`, `execute_bonus`, `atk_flat`, `def_flat`), `combat_tutorial.iniciativa`/`bolinha`, `bonus_ataque`/`bonus_defesa`, `orb.poder`, `skill_info.titulo`, `combat_specials.sem_poderes`, `multidao.switch_titulo`/`poderes_titulo`/`avancar`, `equipar_titulo`/`equipar_corpo` — nos 3 idiomas, com **adaptação livre por idioma** (convenção já estabelecida no projeto pra gíria, ver GDD §13: nunca tradução literal). EN: Skill→Slick, Attack→Venom, Defense→Hide, Powers→Talent(s). ES: Habilidad→Malicia, Ataque→Veneno (coincidência feliz — a palavra em espanhol já era idêntica à nova gíria em português), Defensa→Cuero, Poderes→Talento(s). **Não tocado de propósito**: (1) atributos `F`/`R`/`PdF` em `attr_labels` — são de um sistema abandonado, confirmado morto na auditoria de set/2026 (seção 14 da GDD), fora de escopo. (2) usos genéricos da palavra "ataque"/"attack"/"ataque" como AÇÃO (ex: `normal_attack`, `damage_flat: "Soma +{v} de dano no ataque"`, `log_usou_item: "abriu mão do ataque"`) — só a palavra como NOME DO ATRIBUTO foi trocada, o verbo/ação de atacar continua "ataque" normalmente, senão o texto ficaria sem sentido ("abriu mão do veneno" não faz sentido, "abriu mão do ataque" sim). (3) identificadores internos de código (`onUsarPoder`, `orb.poder` como chave JSON, classe CSS `gang-orb-opt--poder`, tipo de efeito `'atk_flat'`/`'def_flat'`) — só o TEXTO visível ao jogador foi renomeado, nomes de variável/prop/CSS class ficaram como estavam (sem ganho nenhum em renomear, risco real de quebrar referência). **Ajuste no mesmo dia:** o Isaias achou `Veneno` sem graça depois de ver na prática — pediu gírias alternativas pra Ataque, sugeri `Porrada`/`Marra`/`Peso` e ele escolheu **Porrada** (a mais direta — "ir na porrada"). Trocado em `attr_labels.A`, `bonus_ataque` e `skill_desc.atk_flat` nos 3 idiomas — en `Venom`→`Wallop`, es `Veneno`→`Trompada` (`Porrada` não existe em espanhol com esse sentido; `Trompada` é a gíria latino-americana equivalente pra soco/pancada). `Malícia`/`Couro`/`Talento` não mudaram.
- **Gangues é PREQUEL do conto "Alan, o Campeão" — nunca citar o Alan como se a fama dele já fosse fato** (pedido do Isaias, 13/09/2026: "leia o conto do Alan... é a história dele 10 anos depois desse jogo, esse jogo é 10 anos antes dele... quando citar o Alan tem que citar ele que ele ainda vai acontecer... ele é uma coisa do futuro"). Linha do tempo canônica (`ganguesTerritorios.js`, cabeçalho): o jogo se passa na época em que o Retalho (O Costura) quase unificou Marélia sozinho e falhou; só "anos depois" (fora do jogo) o Alan consegue o que o Retalho não conseguiu. O Alan **nunca** aparece como personagem jogável/chefe no jogo — só é citado de leve como nome do futuro. Bug encontrado: dois textos da Pista (`territorios.pista.desc` e `cena.pista.chegada[1]`, i18n×3 + espelho em `docs/Games/Gangues/PISTA_COMUNICACAO.md`) diziam "o Alan pisou aqui"/"o Alan começou do zero igual você", no passado, como se ele já tivesse existido e passado por ali — dava a entender que a fama dele já era fato consumado. Corrigido pra deixar explícito que "a vez dele ainda não chegou" (ele "vai pisar"/"vai começar", futuro). `story.final.paragrafos[2]` já estava certo (fala dele no futuro, "anos depois") — não precisou mexer. Corrigido também um comentário de código errado em `GanguesVictoryFinal.jsx` que dizia "confronto final contra o Alan" — o confronto final de verdade é contra o Retalho, na Laje; o comentário estava simplesmente errado, nunca foi o Alan. Regra pra qualquer texto novo do Gangues que cite o Alan: sempre no futuro/condicional em relação aos eventos do jogo, nunca como fato já acontecido.

- **Gangues: card de "fala final" (xingamento do inimigo antes da tela de vitória/derrota) ficou de fora do passe de retratos — e eu afirmei errado que já estava corrigido sem checar o card específico** (Isaias, 14-15/09/2026, com 2 prints do mesmo bug — "Extensão" e depois "Balconista" ainda mostrando letra em vez de cara). O que aconteceu: uma sessão concorrente fez um passe grande cobrindo o card de KO (`.gang-ko-cena`), ficha (`GanguesFichaCard`), dado dramático e mais 5 telas fora do combate (álbum, recrutamento, escalação, VS da trilha, loja) — mas o overlay `falaFinal` (`useGanguesBattleOutcome.js` → `GanguesCombatOverlays.jsx`, `.gang-fala-final-avatar`) é um card À PARTE, sempre a "última palavra" do inimigo antes do resultado, e nunca fez parte de nenhum dos dois passes. Eu vi que existia um passe de retratos recente e disse ao Isaias que o card dele já devia estar corrigido — **sem testar aquele card específico** — e estava errado; ele reagiu com razão ("testa essa porra antes de falar pra mim que você entregou... me mostre prova, um print"). Fix: `useGanguesBattleOutcome.js` agora busca `getGanguesEnemyPortraitById(inimigo.id)` e grava em `falaFinal.retrato`; `GanguesCombatOverlays.jsx` renderiza `<img>` quando existe (senão cai pra inicial, igual antes); CSS (`Gangues.css`, regra `.gang-fala-final-avatar`) ganhou `overflow:hidden` + `img{object-fit:cover;image-rendering:pixelated}`. **Verificado ao vivo com Playwright antes de reportar como corrigido** (não só relato): fundou gangue, entrou na Pista, chegou em "A boca do sinal", escolheu a opção de treta ("Aperta o pivete"), ligou o modo Automático (`.gang-orb` → `.gang-orb-auto`, atenção: usa `onPointerDown/Up`, não `onClick` — precisa de clique real de ponteiro, `element.click()` via JS não abre o menu) pra resolver a luta sozinha, e o card de nocauteado renderizou a cara de verdade do inimigo — confirmado 2× (Chinelada, depois Luz de Gato), print salvo e enviado. **Lição geral, vale para qualquer "outra sessão já corrigiu isso"**: um passe de correção "os retratos em todo lugar" não é garantia de cobertura total — cada card/overlay que resolve retrato via lookup próprio precisa ser conferido individualmente antes de declarar resolvido, principalmente quando o relato do usuário já provou o contrário com print uma vez.

- **Gangues: "foto do inimigo não aparece" na tela de resultado não era bug de lookup — era opacity de PAI esmagando o filho** (Isaias, 15/09/2026, print da seção "ESTADO FINAL DAS GANGUES" mostrando "Cão Louco" e "Troco Certo" derrotados sem foto, logo depois do fix da fala final). Primeira hipótese óbvia (retrato não encontrado pra esses ids) descartada rápido: os dois estão no `ENEMY_ID_SLUG` (1301/1402) com os PNGs existindo em `assets/enemies/`. Causa real: `.gang-report-member--ko { opacity: 0.35; filter: grayscale(1); }` (`Gangues.css`) aplicava no `<div>` do CARD INTEIRO — `opacity` num elemento pai comprime a renderização do subtree inteiro pra aquele alfa, um filho não consegue "desfazer" isso com o próprio `opacity:1` (multiplica, não substitui). Numa fotinho de 28×28px isso zera o contraste visual a ponto de parecer que a imagem nem carregou. **Verificação antes de mexer**: montei um HTML isolado com os PNGs reais (Cão Louco, Troco Certo, Chinelada) sob a mesma regra CSS exata — os TRÊS sumiram igual, provando que o problema era geral (qualquer inimigo derrotado) e não específico de id/asset, então "de vez em quando" do relato do Isaias na real acontecia sempre, só que ele não tinha reparado em toda derrota. Fix: tirei a opacity do card inteiro; o esmaecimento agora mira só texto (nome/PV a 0.55, letra-fallback a 0.4 quando não tem foto) e a foto em si fica com `grayscale(0.7)` + `opacity: 0.85` (ainda dá pra ver que "morreu" pelo dessaturado, mas a cara continua reconhecível). **Testado ao vivo** (não só no HTML isolado): rodei uma luta de verdade até o fim, veio um inimigo "Trilho" derrotado, print da tela de resultado confirmando a foto nítida. Lição: quando o relato é "às vezes some", checar primeiro se não é uma regra de CSS que afeta TODOS os casos igualmente mas só fica óbvio quando alguém presta atenção — antes de ir atrás de bug de dado/lookup específico por item.

- **Gangues: "a ficha dos inimigos tá desbalanceada" — era verdade, mas a causa raiz não era o número do orçamento** (Isaias, 15/09/2026, relato do amigo dele jogando: "nível 6... apareceu recomendado nível 13 pra lutar com esse cara... eu dei um piau"). Investigação em 3 etapas, cada uma corrigindo a hipótese da anterior:
  1. Achei a origem do "nível 13": não é bug, é `posmuro_2` (guarda-costas do Carvão, gated por reputação, "mais osso do que o resto da rua" — comentário já existia no código), com `nivelRec: 13` autorado de propósito. O aviso (`TretaVS`/`nivel_rec_baixo`) já fazia exatamente o previsto.
  2. Pedido pra checar o Carvão especificamente: o amigo enfrentou ele no nível 11 (real: 34 pontos de ficha) contra o orçamento fixo do chefe (44 pontos, `GANGUES_CHEFE_BUDGET.pista`) e "venceu com certa facilidade" — 77% do orçamento e ainda ganhou fácil, quando o alvo documentado era nível 15. O GDD já registrava isso como pendência não resolvida ("Playtest pra confirmar" depois do ajuste de jan/2027).
  3. Simulação real (Python, reproduzindo `resolveGanguesAction`/iniciativa H+d3 do resolver, sem usar poderes — automático só ataca no normal) provou que o orçamento em si estava CERTO: sem equipamento, o Carvão vencia ~95% das vezes. O furo de verdade: `calcularPontosTime` (toda escala dinâmica do jogo) soma só atributo CRU, nunca o bônus de equipamento (`getGanguesAttributesWithEquip`, que a tela de aviso de nível JÁ usa, mas o gerador de bando não) — e o chefe, sendo orçamento fixo, não tem NENHUMA compensação em lugar nenhum. Simulado: +2A/+1D por ficha (1 arma barata cada) já vira a luta pra ~49%; +4A/+2D (ainda muito acessível) pra ~88% — bate exatamente com "venceu com certa facilidade".
  Decisão do Isaias, ao ser confrontado com a contradição (seus próprios números pedidos pro Carvão, ~22 pontos, eram MENORES que o que já tava no jogo, 26 — aplicar literalmente teria enfraquecido o chefe): em vez de compensar equipamento (mudança grande, afeta os 7 territórios), a Pista virou **NÍVEL FIXO ponta a ponta** — para de perseguir a ficha do jogador de vez. Ladder aprovada: 1º inimigo nível 3 (`beco`/`sinal`/`rinha`, era 4-5), sobe de 3 em 3 nas tretas de rua (`beco_2`=6, `beco_3`=9 — convertidas de `gerarBandoInimigo`/ratio pro mesmo `revezamento` de orçamento fixo por corpo que `beco` já usava), os 2 Generais acima da média (`sinaleiro`=13, `rasteira_velha`=16 — mecanismo NOVO, `poi.fixo`+`poi.pontosFixo` em `GanguesCena.jsx`/`GanguesRoute.jsx`, luta sempre só contra aquele General escalado via `escalarInimigo`, nunca um bando/nunca contra o time do jogador), o galpão pós-muro continua subindo (`posmuro_1`=17, `posmuor_2`=19) e o Carvão fecha fixo em nível 20 (`GANGUES_CHEFE_BUDGET.pista` 44→33, mesma mecânica de sempre — corpos=2/liderFrac=0.6 → Carvão 20 + escolta 13 — só o número mudou, não a lógica). Badge "· NÍVEL 20" do Carvão em `gangues-enemies.json` também atualizado (mostrava 14, ficava mentindo sobre a luta de verdade). **Testado ao vivo antes de reportar**: revelar um POI distante via `window.__ganguesStore.setState` funciona, mas TELEPORTAR o jogador direto pra lá exige a coordenada de MUNDO (`data/cenas/pista/posicoes.js` → `POS_PISTA`), não o `poi.pino` (que é só uma âncora visual em % sobre a arte de fundo, escala completamente diferente — achado na hora, `validPosition()` rejeita y<70 e cai pro spawn quando confundido). Confirmado o Sinaleiro Chefe escalando pros atributos exatos da simulação (A3/H2/D2/PV3/PM3, PVmax12) — bateu certinho. Também achado no processo: pular direto pro Sinaleiro sem passar pelo `beco` primeiro aciona o "amaciamento da 1ª luta da conta" (`primeiraLuta`/`suavizarPrimeiraLuta`, global por conta, não por POI) — inofensivo pra jogador de verdade (sempre bate o `beco` antes), mas quase me fez reportar um número errado no teste; corrigido marcando `__primeiraLutaFeita` antes de teleportar. **Lição geral**: numa reclamação de "tá desbalanceado", separar 3 perguntas antes de mexer em número — (1) esse aviso/número específico é intencional? (2) o mecanismo que produz a dificuldade real tá calculando certo? (3) existe uma variável de poder do jogador (aqui: equipamento) que nenhuma conta de balanceamento considera? Só a 3ª pergunta tinha uma resposta de bug de verdade — as duas primeiras eram comportamento correto sendo mal-interpretado.
  Os outros 6 territórios (ainda formato antigo, sem cena) continuam com a escala antiga (ratio contra o jogador, sem contar equipamento) até passarem pelo mesmo tratamento — o furo de `calcularPontosTime` ignorar equipamento ainda existe pra eles, só não é mais problema pra Pista.

- **Gangues: piso da ladder de nível fixo da Pista tava baixo demais na 1ª leva — Isaias jogou e mandou print da ficha real** (15/09/2026, logo depois do lançamento da ladder acima: "beco" saiu com "Ratazana" 2 pontos e "Luz de Gato" 3 pontos numa dupla — "não é nem nível pra isso"). Causa exata: escolhi nível 3 pro "1º inimigo" sem considerar que `gerarBandoRevezamento` desconta ×0.75 por corpo quando sai dupla (`chanceDupla`) — 3×0.75≈2, praticamente zero. Regra dada pelo Isaias, direta: "quando for 1 tem que ser de 8 pontos pelo menos; quando for 2, um uns 8 e outro uns 6." Com `budgetPorCorpo=8`, o desconto de dupla JÁ dá round(8×0.75)=6 — bate exatamente com o pedido, sem precisar mexer no multiplicador (`ganguesEncontros.js`) nem em mecanismo nenhum, só o número autorado por POI. Ladder inteira rebaseada com piso 8: `sinal`/`beco`/`rinha` 3→8, `beco_2` 6→10, `beco_3` 9→12, Sinaleiro Chefe 13→15, Rasteira Velha 16→17, `posmuro_1` 17→18 — `posmuro_2` (19) e o Carvão (fixo em 20) ficaram como estavam, já validados por simulação de combate real na leva anterior, e ainda sobra folga suficiente entre eles e o resto da rua depois do rebase. **Lição**: ao definir um "piso" pra uma faixa de nível, sempre rodar a conta através de QUALQUER desconto/multiplicador que o mecanismo aplique depois (aqui, o desconto de dupla) antes de aprovar o número — "nível 3" parecia razoável isolado, mas confrontado com um mecanismo do sistema (não visível só olhando o número em si) virava quase zero.

## Regra Anti-Over-Engineering

Antes de criar **mais de 2 arquivos novos** para resolver qualquer problema, o agente deve apresentar a proposta e aguardar aprovação explícita.

## Regra de Tamanho de Arquivo

Todo arquivo tocado com **mais de 300 linhas** deve ser avaliado para extração antes de adicionar mais código. Propor a extração, não executar sem aprovação.

**Exceção documentada:** `src/pages/games/Gangues/data/ganguesEncontros.js` (356 linhas, avaliado em set/2026 no `PLANO_REFATORACAO_ARQUIVOS_GRANDES_GANGUES_2026-09-11.md` §1). Não divide: é um módulo único e coeso (geração de bando inimigo), não uma mistura de concerns — o tamanho vem de tabelas de configuração fortemente comentadas (histórico de calibração, resultado de simulação), não de responsabilidades empilhadas. Dividir config de lógica ali seria corte artificial sem ganho de legibilidade.

## Segurança é hábito, não fase

Nenhum commit pode passar com:
- String hardcoded visível ao usuário
- CSS inline (`style={{}}` para propriedades visuais)
- Chave i18n faltando em **PT/EN/ES**
- Insert direto em tabela Supabase fora do listener correto

## Este documento é trabalho em progresso

AGENTS.md **deve crescer** a cada problema novo resolvido no projeto. A cada task concluída com uma decisão relevante de arquitetura, o agente **deve propor adição** ao AGENTS.md.

## Architecture notes

- **Lobby multiplayer compartilhado é a entrada oficial dos jogos** — a rota `/games/multiplayer/lobby` e `useSharedLobbyMachine` cuidam apenas de acionar a fila oficial do jogo, observar a sala devolvida e encaminhar os usuários confirmados. Cada game aponta seu menu para essa rota com `game` e `mode`, fornece um adaptador próprio e mantém sua máquina de partida independente. Presence não é fonte de verdade para matchmaking. No Top Trumps, `entrar_fila_publica` é a autoridade para criar/conciliar a sala; o cliente nunca procura e reutiliza uma sala anterior antes da RPC. A rota legada `/games/toptrumps/lobby` existe somente como redirect de compatibilidade.
- **Lobby multiplayer: encontro não navega imediatamente** — `MATCHED` inicia uma preparação visual de 5 segundos controlada por `useSharedLobbyMachine`. A UI informa que o rival foi encontrado e que os jogadores estão sendo sincronizados; o adaptador do jogo só recebe `onMatch(salaId)` quando a contagem chega a zero. IDs de sala, chaves de fila e linguagem de diagnóstico ficam apenas nos logs, nunca na interface final.
- **Top Trumps MP: state machine é a autoridade única do turno** — mudanças de fase, início/fim da apresentação do resultado, confirmações dos jogadores e liberação da próxima rodada devem passar exclusivamente por `useMultiplayerTurnMachine`. Eventos do Supabase, presença, animações e timers apenas despacham eventos; é proibido alterar a fase diretamente ou manter um segundo cronômetro de resultado fora da máquina. Os dois jogadores devem assinar o mesmo tópico de presença por sala (`presenca-partida-${salaId}`); incluir `userId` no nome separa os clientes e impede confirmações mútuas.
- **Top Trumps MP: barreira curta antes da escolha** — ao entrar em `jogando` ou mudar `turno_atual`, a interação permanece bloqueada por 2 segundos para conciliar turno, carta e autoridade entre os clientes. O cronômetro de escolha de 30 segundos começa somente depois dessa liberação. Não remover a barreira sem substituir sua garantia de consistência.
- **Top Trumps MP: URLs de salas encerradas são efêmeras** — a tela final registra em `sessionStorage` um prazo local de 30 segundos por `salaId`. Durante esse prazo, reload na mesma aba preserva o relatório; sem prazo ativo ou depois de expirar, qualquer acesso a uma sala `encerrada` redireciona imediatamente para `/games/toptrumps`. O botão final e o retorno automático também removem o prazo e voltam ao início do Top Trumps, nunca ao catálogo `/games`.
- **Top Trumps MP: PPT tem prazo obrigatório** — cada cliente dispõe de 30 segundos para escolher pedra, papel ou tesoura. No zero, o próprio cliente envia uma opção aleatória por `escolherPPT`; o guard síncrono `pptEscolhiRef` impede envio manual e automático duplicado.

- **Top Trumps: artes oficiais centralizadas** — toda arte de personagem deve usar o nome `card-NN.png` em `src/assets/images/cards/characters/`, com `NN` correspondente ao `id` existente nos catálogos `supertrunfo-pt.json`, `supertrunfo-en.json` e `supertrunfo-es.json`. Componentes devem resolver a imagem exclusivamente por `src/lib/topTrumpsCardImages.js`; é proibido recriar mapas manuais de imports por card. Ao adicionar uma arte, validar que os IDs dos três catálogos coincidem, que o resolvedor encontra todas as imagens e que nenhuma cai em `card-fallback.png`.
- **Game pages** (`/games/ldi`, `/games/jackcandy`, `/games/toptrumps`, etc.) have their own data/, store/, components/ subdirectories with independent Zustand stores. They do not share state.
- **i18n** uses `LanguageContext` (persisted as `ldi-locale` in localStorage) with JSON files in `src/i18n/`. The `t("key.path")` function resolves translations.
- **ReaderContext** wraps the app — when `readerMode` is true, Navbar and TrialBanner are hidden (used by WebtoonEpisodio and LivroCapitulo).
- **z-index layers** are defined and must not collide: SearchModal (2000), AchievementToast (1500), Navbar (1000), TrialBanner (998), CookieBanner (200), LDINotification (150), ScrollToTop (100), MusicSection dropdown (50).
- **Book chapters** are `.md` files in `src/data/livro/{lang}/` loaded via `import.meta.glob`. Publication control is in `src/data/livro-index.json` (`publicado: true/false`).
- **Webtoon pages** live in `public/webtoon/` (not `src/assets/`) because they need direct URL access.
- **All other assets** go in `src/assets/` and are imported (Vite processes and hashes them).
- **Supabase migrations** are in `supabase/migrations/`. Files 001-003 exist only on the remote database; the repo has `004_jack_v3.sql` and `005_pesadelo_particular.sql`. All tables use RLS with `auth.uid() = user_id`.
- **Fichas system** — virtual currency managed by `FichasContext`. Games gate access via `useFichaGate` hook and `ModalSemFichas` component.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- Dirty graphify-out/ files are expected after hooks or incremental updates; dirty graph files are not a reason to skip graphify. Only skip graphify if the task is about stale or incorrect graph output, or the user explicitly says not to use it.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
